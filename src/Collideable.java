/**
 * Interface for classes which take specific action when they collide with a character
 *
 * @author Victoria Halim
 */

public interface Collideable {

    /**
     * Performs a collision action on character.
     */
    void collisionAction(Character character);
}
